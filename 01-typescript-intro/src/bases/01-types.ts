export let name: string | number = 'Enrique';
//Puedo declarar más de un tipo de dato a una variable.
export const age = 35;
export const isValid: boolean = true;

//Generalmente a las constante no es necesario colocarles de qué tipo son, ya que siempre valdrán lo mismo, entonces da igual.

name = 20;
